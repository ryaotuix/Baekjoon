#include <bits/stdc++.h>

using namespace std;
typedef pair<int, int> pii;

int main()
{
    int n; cin >> n;
    int xh, yh, xw, yw; cin >> xh >> yh >> xw >> yw;
    vector<pii> errands;
    
    for (int i = 0; i < n; i++)
    {
        pii p;
        cin >> p->first;
        cin >> p->second;
        errand.push_back(p);
    }


}