#include <bits/stdc++.h>
#include <vector>

using namespace std;

int main()
{
    vector<char> tasks = {'A', 'A', 'A', 'B', 'B', 'B'};
    int n = 2;

    unordered_map<char, int> umap;
    







    // unordered_map<char, int> umap;

    // int freq = 0, numhighest = 0;

    // // finding max frequency and also add each char to its values
    // for (auto e : tasks)
    // {
    //     freq = max(freq, ++umap[e]);
    // }
    
    // // get how many characters have same frequency
    // for (auto e : umap) if (e.second == freq) numhighest++;
    // cout << max((int) tasks.size(), numhighest + ((freq-1) * (n+1)) );
} 