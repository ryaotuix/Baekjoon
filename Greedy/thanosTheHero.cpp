#include <bits/stdc++.h>

using namespace std;


vector<int> universes;

int main()
{
    int N; cin >> N;

    for (int i = 0; i < N; i++)
    {
        int pop;
        universe.push_back(pop);
    }

    int len = universe.size();
    
}